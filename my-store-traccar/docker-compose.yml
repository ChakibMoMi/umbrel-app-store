version: "3.7"

services:
  app:
    image: traccar/traccar:6.2
    restart: on-failure
    ports:
      - "${APP_PORT}:8082"
    volumes:
      - "${APP_DATA_DIR}/data/logs:/opt/traccar/logs"
      - "${APP_DATA_DIR}/data/data:/opt/traccar/data"
