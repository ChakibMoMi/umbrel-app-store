version: "3.7"

services:
  app:
    image: b4bz/homer:24.02.1
    restart: on-failure
    ports:
      - "${APP_PORT}:8080"
    volumes:
      - "${APP_DATA_DIR}/data/assets:/www/assets"
    environment:
      - INIT_ASSETS=1
