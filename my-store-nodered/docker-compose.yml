version: "3.7"

services:
  app:
    image: nodered/node-red:3.1.7
    restart: on-failure
    ports:
      - "${APP_PORT}:1880"
    environment:
      - TZ=Europe/Berlin
    volumes:
      - "${APP_DATA_DIR}/data:/data"
