version: "3.7"

services:
  app:
    image: lscr.io/linuxserver/grocy:4.2.0
    restart: on-failure
    ports:
      - "${APP_PORT}:80"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Berlin
    volumes:
      - "${APP_DATA_DIR}/data:/config"
